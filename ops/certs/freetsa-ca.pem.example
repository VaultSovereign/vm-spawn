# Placeholder CA bundle for RFC3161 verification (do not use in production).
# Copy your TSA CA here as 'freetsa-ca.pem' and verify its fingerprint out-of-band.
# 
# Download FreeTSA CA:
#   wget https://freetsa.org/files/cacert.pem -O ops/certs/freetsa-ca.pem
# 
# Verify fingerprint (check FreeTSA website):
#   openssl x509 -in ops/certs/freetsa-ca.pem -noout -fingerprint -sha256

